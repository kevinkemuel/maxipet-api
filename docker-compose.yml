version: "3.9"

services:
  api:
    build: .
    container_name: maxipet-api
    ports:
      - "8080:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    restart: unless-stopped
